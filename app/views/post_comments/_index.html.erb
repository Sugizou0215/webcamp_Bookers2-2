<thead>
  <tr>
    <th>User</th>
    <th>Comment</th>
    <th></th>
  </tr>
</thead>
<% book.post_comments.each do |post_comment| %>
  <tr>
    <td>
      <%= attachment_image_tag post_comment.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg"%></br>
      <%= link_to post_comment.user.name, user_path(post_comment.user_id) %>
    </td>
    <td><%= post_comment.comment %></td>
    <% if post_comment.user == current_user %>
      <div class="comment-delete">
        <td>
          <%= link_to "Destroy", book_post_comment_path(post_comment.book, post_comment), method: :delete, class:"btn btn-sm btn-danger", remote: true %>
        </td>
      </div>
    <% end %>
  </tr>
<% end %>